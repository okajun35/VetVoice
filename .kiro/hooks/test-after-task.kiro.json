{
  "name": "Run Tests After Task",
  "version": "1.0.0",
  "description": "specタスク完了後にテストスイートを実行",
  "when": {
    "type": "postTaskExecution"
  },
  "then": {
    "type": "runCommand",
    "command": "npx vitest --run"
  }
}